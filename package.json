{
  "name": "yatt",
  "version": "0.0.1",
  "author": "buribullet@gmail.com",
  "devDependencies": {
    "esbuild": "^0.20.2",
    "rollup": "^2.79.1",
    "rollup-plugin-esbuild": "^4.10.3",
    "tap": "^18.7.1",
    "ts-node": "^10.1.0",
    "typescript": "^4.4.2"
  },
  "description": "Yet Another Template Transpiler to TypeScript",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "bun run --cwd core/lrxml build && bun run --cwd core/codegen0 build",
    "test": "bun run tap"
  },
  "workspaces": [
    "core/lrxml",
    "core/codegen0",
    "adapter/express",
    "util/xhf"
  ],
  "tap": {
    "disable-coverage": true
  }
}
